#include <bits/stdc++.h>
using namespace std;
int num[2001];

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, m;
    cin >> n;
    for(int i=1; i<=n; i++)
        cin >> num[i];




    cin >> m;
    while(m--) {
        int s, e;
        cin >> s >> e;

    }
}